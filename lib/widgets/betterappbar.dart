import 'package:flutter/material.dart';

class BetterAppBar extends StatelessWidget {
  const BetterAppBar({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return AppBar();
  }
}
